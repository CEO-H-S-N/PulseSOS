PORT=5000
NODE_ENV=development

# Firebase service account credential JSON string format
# Run: cat service-account.json | tr -d '\n' to compress for env
FIREBASE_SERVICE_ACCOUNT={"type":"service_account","project_id":"..."}
FIREBASE_DATABASE_URL=https://pulsesos-default-rtdb.firebaseio.com
